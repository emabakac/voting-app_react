import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  container: {
    display: "flex",
    width: "100%",
    justifyContent: "center",
    margin: "30px 0",
  },
});
